# Dotfiles for Windows Subsystem for Linux

## .bashrc

Copy to `~/.bashrc`;

```
$ cp ./.bashrc ~/.bashrc
```

## .inputrc

Copy to `~/.inputrc`;

```
$ cp ./.inputrc ~/.inputrc
```

## .vimrc

Copy to `~/.vimrc`;

```
$ cp ./.vimrc ~/.vimrc
```
